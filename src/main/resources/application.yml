spring:
  logging:
    level:
      root: INFO

    pattern:
      console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

    file:
      name: logs/trass-automation.log
  data:
    redis:
      host: ${SPRING_REDIS_HOST:localhost}
      port: ${SPRING_REDIS_PORT:6379}
